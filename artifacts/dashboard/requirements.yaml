packages:
  "framer-motion": "For smooth layout transitions and terminal-style entry animations"
  "date-fns": "For formatting history timestamps"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility to merge tailwind classes without style conflicts"

images: []

notes:
  - Using manual fetch hooks instead of generated ones as requested
  - Custom UI components built to match terminal/hacker aesthetic perfectly
  - Polling WhatsApp status every 3s as requested
